[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers

markers =
    unit: fast unit tests (no network, no external deps)
    contract: output/format invariants (no network)
    integration: slow tests that may touch external systems
